<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ meta_title|default('EBL Tours - The Iguazu Falls Double Experience') }}</title>
    <meta name="description" content="{{ meta_description|default('Experience the breathtaking Iguazu Falls with EBL Tours. Professional guides, safe travel, and unforgettable memories.') }}">
    <meta name="keywords" content="{{ meta_keywords|default('Iguazu Falls, Foz do Iguaçu, Tours, Travel, Brazil, Argentina') }}">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ app.request.uri }}">
    <meta property="og:title" content="{{ meta_title|default('EBL Tours') }}">
    <meta property="og:description" content="{{ meta_description|default('Experience the breathtaking Iguazu Falls with EBL Tours.') }}">
    <meta property="og:image" content="{{ app.request.schemeAndHttpHost }}/assets/images/og-image.jpg">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="{{ app.request.uri }}">
    <meta property="twitter:title" content="{{ meta_title|default('EBL Tours') }}">
    <meta property="twitter:description" content="{{ meta_description|default('Experience the breathtaking Iguazu Falls with EBL Tours.') }}">
    <meta property="twitter:image" content="{{ app.request.schemeAndHttpHost }}/assets/images/og-image.jpg">

    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#f0fdf4',
                            500: '#22c55e',
                            600: '#16a34a',
                            700: '#15803d',
                            800: '#166534',
                            900: '#14532d',
                        },
                        yellow: {
                            400: '#facc15',
                            500: '#eab308',
                        }
                    },
                    fontFamily: {
                        sans: ['Montserrat', 'system-ui', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    
    <!-- Custom CSS -->
    <style>
        .btn-primary {
            @apply bg-primary-600 hover:bg-primary-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-105;
        }
        
        .btn-secondary {
            @apply bg-white hover:bg-gray-50 text-primary-600 font-semibold py-3 px-6 rounded-lg border border-primary-600 transition-all duration-200;
        }
        
        .section-padding {
            @apply py-16 px-4 sm:px-6 lg:px-8;
        }
        
        .container-custom {
            @apply max-w-7xl mx-auto;
        }

        .hero-overlay {
            background: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.6));
        }

        .card-hover {
            @apply transition-all duration-300 hover:shadow-xl hover:scale-105;
        }

        .lazy-load {
            opacity: 0;
            transition: opacity 0.3s;
        }

        .lazy-load.loaded {
            opacity: 1;
        }
    </style>

    {% block head %}{% endblock %}
</head>
<body class="font-sans">
    <!-- Header -->
    <header class="fixed top-0 left-0 right-0 z-50 transition-all duration-300 bg-white shadow-lg">
        <!-- Top bar -->
        <div class="bg-primary-600 text-white py-2 px-4">
            <div class="container-custom flex justify-between items-center text-sm">
                <div class="flex items-center space-x-4">
                    <div class="flex items-center space-x-1">
                        <i class="fas fa-phone w-4 h-4"></i>
                        <span>+55 (45) 99999-9999</span>
                    </div>
                    <div class="flex items-center space-x-1">
                        <i class="fas fa-envelope w-4 h-4"></i>
                        <span>info@ebl-tours.com</span>
                    </div>
                </div>
                <div class="hidden md:block">
                    <span>The Iguazu Falls Double Experience</span>
                </div>
            </div>
        </div>

        <!-- Main navigation -->
        <nav class="py-4">
            <div class="container-custom flex justify-between items-center">
                <div class="flex items-center">
                    <a href="{{ path('homepage') }}">
                        <img src="/assets/images/ebl-logo.png" alt="EBL Tours" class="h-12 w-auto">
                    </a>
                </div>

                <!-- Desktop menu -->
                <div class="hidden md:flex items-center space-x-8">
                    <a href="{{ path('homepage') }}" class="text-gray-700 hover:text-primary-600 font-medium transition-colors">
                        Home
                    </a>
                    <a href="{{ path('tours_listing') }}" class="text-gray-700 hover:text-primary-600 font-medium transition-colors">
                        Tours
                    </a>
                    <a href="{{ path('itinerary') }}" class="text-gray-700 hover:text-primary-600 font-medium transition-colors">
                        Itinerary
                    </a>
                    <a href="{{ path('contact') }}" class="btn-primary">
                        Contact
                    </a>
                </div>

                <!-- Mobile menu button -->
                <button class="md:hidden p-2" id="mobile-menu-btn">
                    <i class="fas fa-bars w-6 h-6"></i>
                </button>
            </div>

            <!-- Mobile menu -->
            <div class="md:hidden bg-white border-t border-gray-200 hidden" id="mobile-menu">
                <div class="py-4 space-y-2">
                    <a href="{{ path('homepage') }}" class="block w-full text-left px-4 py-2 text-gray-700 hover:bg-gray-50">
                        Home
                    </a>
                    <a href="{{ path('tours_listing') }}" class="block w-full text-left px-4 py-2 text-gray-700 hover:bg-gray-50">
                        Tours
                    </a>
                    <a href="{{ path('itinerary') }}" class="block w-full text-left px-4 py-2 text-gray-700 hover:bg-gray-50">
                        Itinerary
                    </a>
                    <a href="{{ path('contact') }}" class="block w-full text-left px-4 py-2 text-primary-600 font-medium">
                        Contact
                    </a>
                </div>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="pt-24">
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white">
        <div class="container-custom section-padding">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
                <!-- Company Info -->
                <div class="lg:col-span-2">
                    <div class="flex items-center mb-4">
                        <img src="/assets/images/ebl-logo-white.png" alt="EBL Tours" class="h-12 w-auto">
                    </div>
                    <p class="text-gray-300 mb-6 leading-relaxed">
                        EBL Tours specializes in providing unforgettable experiences at Iguazu Falls and surrounding attractions. 
                        With over 10 years of experience, we ensure safe, professional, and memorable adventures.
                    </p>
                    <div class="flex space-x-4">
                        <a href="#" class="bg-primary-600 hover:bg-primary-700 p-2 rounded-full transition-colors">
                            <i class="fab fa-facebook-f w-5 h-5"></i>
                        </a>
                        <a href="#" class="bg-primary-600 hover:bg-primary-700 p-2 rounded-full transition-colors">
                            <i class="fab fa-instagram w-5 h-5"></i>
                        </a>
                        <a href="#" class="bg-primary-600 hover:bg-primary-700 p-2 rounded-full transition-colors">
                            <i class="fab fa-twitter w-5 h-5"></i>
                        </a>
                        <a href="#" class="bg-primary-600 hover:bg-primary-700 p-2 rounded-full transition-colors">
                            <i class="fab fa-youtube w-5 h-5"></i>
                        </a>
                    </div>
                </div>

                <!-- Quick Links -->
                <div>
                    <h3 class="text-lg font-semibold mb-4">Quick Links</h3>
                    <ul class="space-y-2">
                        <li><a href="{{ path('homepage') }}" class="text-gray-300 hover:text-primary-400 transition-colors">Home</a></li>
                        <li><a href="{{ path('tours_listing') }}" class="text-gray-300 hover:text-primary-400 transition-colors">Tours</a></li>
                        <li><a href="{{ path('itinerary') }}" class="text-gray-300 hover:text-primary-400 transition-colors">Itinerary</a></li>
                        <li><a href="{{ path('contact') }}" class="text-gray-300 hover:text-primary-400 transition-colors">Contact</a></li>
                    </ul>
                </div>

                <!-- Contact Info -->
                <div>
                    <h3 class="text-lg font-semibold mb-4">Contact</h3>
                    <div class="space-y-3">
                        <div class="flex items-start space-x-3">
                            <i class="fas fa-map-marker-alt text-primary-400 mt-1 flex-shrink-0"></i>
                            <div class="text-gray-300 text-sm">
                                Av. das Cataratas, 1234<br>
                                Foz do Iguaçu, PR<br>
                                Brazil
                            </div>
                        </div>
                        <div class="flex items-center space-x-3">
                            <i class="fas fa-phone text-primary-400 flex-shrink-0"></i>
                            <div class="text-gray-300 text-sm">+55 (45) 99999-9999</div>
                        </div>
                        <div class="flex items-center space-x-3">
                            <i class="fas fa-envelope text-primary-400 flex-shrink-0"></i>
                            <div class="text-gray-300 text-sm">info@ebl-tours.com</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Bottom Bar -->
            <div class="border-t border-gray-800 mt-12 pt-8">
                <div class="flex flex-col md:flex-row justify-between items-center">
                    <p class="text-gray-400 text-sm mb-4 md:mb-0">
                        © {{ "now"|date("Y") }} EBL Tours. All rights reserved.
                    </p>
                    <div class="flex space-x-6 text-sm">
                        <a href="#" class="text-gray-400 hover:text-primary-400 transition-colors">Privacy Policy</a>
                        <a href="#" class="text-gray-400 hover:text-primary-400 transition-colors">Terms of Service</a>
                        <p class="text-gray-400">Developed by <a href="#" class="text-primary-400 hover:text-primary-300">WebDev Team</a></p>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- JavaScript -->
    <script>
        // Mobile menu toggle
        document.getElementById('mobile-menu-btn').addEventListener('click', function() {
            const menu = document.getElementById('mobile-menu');
            menu.classList.toggle('hidden');
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Lazy loading for images
        const lazyImages = document.querySelectorAll('.lazy-load');
        const imageObserver = new IntersectionObserver((entries, observer) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const img = entry.target;
                    img.src = img.dataset.src;
                    img.classList.add('loaded');
                    observer.unobserve(img);
                }
            });
        });

        lazyImages.forEach(img => imageObserver.observe(img));
    </script>

    {% block scripts %}{% endblock %}
</body>
</html>